# XcodeGen definition — run `xcodegen generate` on macOS to produce CornerCast.xcodeproj
# (brew install xcodegen)
name: CornerCast
options:
  bundleIdPrefix: dev.cornercast
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "2"          # iPad only
targets:
  CornerCast:
    type: application
    # iPadOS + macOS(Mac Catalyst)。UIKit/シーンAPIを共有する単一コードベース。
    supportedDestinations: [iOS, macCatalyst]
    sources: [CornerCast]
    settings:
      base:
        GENERATE_INFOPLIST_FILE: false
        INFOPLIST_FILE: CornerCast/Info.plist
  CornerCastTests:
    type: bundle.unit-test
    supportedDestinations: [iOS, macCatalyst]
    sources: [CornerCastTests]
    dependencies:
      - target: CornerCast

# CI(xcodebuild -scheme CornerCast test)が使う明示的スキーム
schemes:
  CornerCast:
    build:
      targets:
        CornerCast: all
        CornerCastTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - CornerCastTests
